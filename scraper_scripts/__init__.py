# Questo file dice a Python che la cartella `scraper_scripts` può essere trattata 
# come un "package" (un modulo che contiene altri moduli). 
# Questo ci aiuterà con le importazioni nello script principale.